## Sprint-1 10th June 2026 - 13th June 2026     Basic project setup
- [X] Setup a minimal dockerized container for nodejs development with nestcli and other required development tools and postgres installed. Make sure the container only comes live if postgres is running and is active. Test the connection to the container from terminal as well as vscode.
    - [X] Setup a minimal dockerized container for nodejs development with git and postgres installed. Make sure the container only comes live if postgres is running and is active. Test the connection to the container from terminal as well as vscode.
    - [X] Setup a minimal dockerized container for nodejs development with nestcli.
- [ ] Bootstrap the nestjs project by logging into the container as nestcli will be already available there.
- [ ] Create and test auth module and setup RBAC
- [ ] Setup other portions of the complete techstack to proceed the development further in the next sprints. 
